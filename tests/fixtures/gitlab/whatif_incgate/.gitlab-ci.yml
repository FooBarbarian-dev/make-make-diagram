# Conditional include: web-only.yml exists only in pipelines where the
# include gate matches — its jobs must not leak into push pipelines.

include:
  - local: web-only.yml
    rules:
      - if: $CI_PIPELINE_SOURCE == "web"

always_job:
  script:
    - echo always
